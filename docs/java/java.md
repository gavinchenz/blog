---



title: 后端开发自学之路 - java
sidebar: auto
---

# java 自学笔记

学习资料来自B站UP主**黑马程序员**相关课程。 [*传送门*](https://www.bilibili.com/video/BV17F411T7Ao?p=1&vd_source=4766936e0680cf517a75c2f16b3d2394)

## JAVA 入门

### 认识CMD

#### 什么是 cmd ?

就是在windows操作系统中，利用命令行的方式去操作计算机。

我们可以利用cmd命令去操作计算机，比如：打开文件，打开文件夹，创建文件夹等。

打开 cmd： `win + R`  输入 `cmd` 

打开 **我的电脑**： `win + E`

#### 常见cmd 命令

- 盘符名称 + 冒号   ---- 用于盘符切换

> eg：  ` E:`   回车，表示切换到 E 盘

- dir  ---- 查看当前路径下的内容（包含被隐藏的文件及文件夹）

- cd 目录 ---- 进入到单极目录

> eg：`cd SuperMap`  指进入到当前目录下的 SuperMap 文件夹下

- cd ..     ---- 回到上一级目录
- cd 目录1\目录2\ ...    ---- 进入到多级目录下

> eg：cd SuperMap\iServer\bin-

- cd \  ---- 回到当前所属盘符根目录

- cls ---- 清屏
- exit  ---- 退出cmd

### 环境变量配置

#### 配置环境变量：

1. `win + E`  打开我的电脑
2. 空白处鼠标右键点击“属性”
3. 左键点击选择“高级系统设置”
4. 左键点击“环境变量”
5. 在系统变量中找到“Path”

![配置环境变量](./images/000.png "配置环境变量")

为什么要配置环境变量?
我们**想要在任意的目录下都可以打开指定的软件**。就可以把软件的路径配置到环境变量中。

#### 自定义环境变量配置

1. win + E`  打开我的电脑
2. 空白处鼠标右键点击“属性”
3. 左键点击选择“高级系统设置”
4. 左键点击“环境变量”
5. 在系统变量中点击“新建”
6. 自定义变量名和变量值，如：变量名为：`JAVA_HOME`，变量值为`H:\java\2-develop\jdk17`![自定义变量名和变量值](./images/004.png "自定义变量名和变量值")
7. 再在 Path 中配置

![自定义Path 中配置](./images/005.png "自定义Path 中配置")

###  初识 JDK

JDK 是 JAVA 代码编译及运行的工具。

官网：[`http://www.oracle.com`](http://www.oracle.com)

下载地址： [`https://www.oracle.com/java/technologies/downloads/`](https://www.oracle.com/java/technologies/downloads/)

**注意1**：针对不同的操作系统，需要下载对应版本的JDK。

**注意2**：如果你的电脑是windows32位的，建议重装系统，重装成64位的操作系统。

​              因为 Java 从 9 版本开始，就已经不提供32位版本的安装包了。

安装建议：

> 建议1: 安装路径中不要包含中文和空格
>
> 建议2: 所有的开发工具最好安装目录统一

目录结构

![目录结构](./images/001.png "目录结构")

目录结构说明

![目录结构说明](./images/002.png "目录结构说明")

### JAVA 语言

#### JAVA 发展史

![JAVA 发展史](./images/006.png "JAVA 发展史")

#### JAVA 适用平台

![JAVA 三大使用平台](./images/007.png "JAVA 三大使用平台")

1.**Java SE**

![JAVA SE](./images/008.png "JAVA SE")

2.**JAVA ME**

![JAVA ME](./images/009.png "JAVA ME")

3. **JAVA EE**

![JAVA EE](./images/010.png "JAVA EE")

#### JAVA 能做什么

![JAVA 作用](./images/011.png "JAVA 6大适用场景")

#### JAVA 的主要特性

- 面向对象
- 安全性
- 多线程
- 简单易用
- 开源
- 跨平台

![JAVA 特性](./images/012.png "JAVA 特性")

#### JAVA跨平台原理

![JAVA跨平台原理](./images/013.png "JAVA跨平台原理")

### JRE 和 JDK

#### JVM 虚拟机

JVM (Java Virtual  Machine)：即 Java 虚拟机，是真正运行 Java 程序的地方

#### JDK 开发工具包

JDK (Java Development kit)：即 java 开发工具包，一般会包含三个部分：**JVM+ 核心类库 + 开发工具**

![JDK 开发工具包](./images/014.png "JDK 开发工具包")

#### JRE 运行环境包

JRE (Java Runtime Environment)：即 java 的运行环境包，是仅保存开发工具中需要和必须的工具，组成的运行环境包。

![JRE 运行环境包](./images/015.png "JRE 运行环境包")

#### 总结

![总结](./images/016.png "总结")

## JAVA 基础知识

### 注释

> 注释是在程序指定位置添加的说明性信息
>
> 简单理解，就是对代码的一种解释

注释的分类

![注释的分类](./images/017.png "注释的分类")

**注1：被注释的内容是不参与编译且不参与运行的，仅仅是对代码的解释说明**

**注2：不论是那种形式的注释，在书写时都不要进行注释的嵌套**

### 关键字

即被 JAVA  赋予了**特定含义的英文单词**

>特点：
>
>1.关键字的字母全部小写
>2.常用的代码编辑器，针对关键字有特殊的颜色标记，非常直观。

class：用于（创建/定义）一个类，而类是 JAVA 中最基本的组成单元

### 字面量

字面量，即数据在程序中的书写格式

字面量的基础分类：

![字面量的分类](./images/018.png "字面量的分类")

特殊字符（特殊字面量）

>`\t`、`\r`、`\n`  ...
>
>`\t` 制表符  ---- 指在打印的时候，把前面字符串的长度补齐到8，或者8的整数倍。最少补1个空格，最多补8个空格

### 变量

变量：是指在程序的执行过程中，用于存储且其值有可能发生改变的量（数据）

变量的定义格式：

> 数据类型  变量名 = 数据值;

![变量的定义格式](./images/019.png "变量的定义格式")

变量的适用方式：

![变量的使用方式](./images/020.png "变量的使用方式")

使用变量时的注意事项：

>1.变量只能存一个值
>
>2.变量名不允许重复定义
>
>3.一条语句可以定义多个变量
>
>4.变量在使用之前一定要进行赋值
>
>5.变量的使用有作用域范围

### 计算机的存储规则

#### 常见的进制

- 二进制： 由 0 和 1 组成，代码中以 `0b` 开头
- 十进制： 由 0~9 组成，正常书写不加任何前缀
- 八进制： 由 0~7 组成，代码中以 `0`开头
- 十六进制： 由 0~9 及 a~f 组成，代码中以`0x`开头

> 上述开头规则均是 JDK7 的新特性，因此在 JDK7 及其以上版本均支持

##### 进制与文本存储

在计算机中，任意数据都是以二进制的形式来存储的**

![计算机中的存储规则](./images/028.png "计算机中的存储规则")

二进制的计算规则：

1. 逢二进一
2. 借一当二

![二进制的运算过程](./images/021.png "二进制的运算过程")

#### 任意进制转十进制

**公式：** `系数 * 基数的权次幂 再相加`

系数：指每一位上的数

基数：指当前 进制数

权： 从右往左，依次为 0 1 2 3 4 5 ...

##### 二进制转十进制

![二进制转十进制](./images/022.png "二进制转十进制")

##### 八进制转十进制

![八进制转十进制](./images/023.png "八进制转十进制")

##### 十六进制转十进制

![十六进制转十进制](./images/024.png "十六进制转十进制")

##### 8421快速转换法（二进制转十进制）

![8421快速转换法](./images/025.png "8421快速转换法")

#### 十进制转其他进制

方法： **除基取余法**，即不断的除以基数（几进制，基数就是几）得到的余数，直到商为 0 ，再将余数倒着拼接起来即可。

![举例：十进制转二进制](./images/026.png "举例：十进制转二进制")

##### ASCII 码表

ASCII ( American Standard Code for Information Interchange ) 美国信息交换标准码表 

![ASCII码表](./images/027.png "ASCII码表")

以上即为数字、字母、汉字等文本信息的存储规则，接下来一起看下图片存储规则：

##### 图片存储

图片数据分类：

- 黑白图    ---- 用 1 和 0 来表示图片信息，1 为白色 0 为黑色
- 灰度图    ---- 用 0~255 来表示灰度图片信息，根据数值的大小不同，它的灰度程度不同
- 彩色图    ---- 在单位像素点上的红、绿、蓝三色的颜色占比来表示及存储的图片信息

![图片数据分类](./images/029.png "图片数据分类")

分辨率，指纵横向上的像素点数，单位是px。屏幕分辨率确定计算机屏幕上显示多少信息的设置，以水平和垂直像素来衡量。

![分辨率](./images/030.png "分辨率")

![单位像素与分辨率](./images/031.png "单位像素与分辨率")

![灰度图](./images/032.png "灰度图")

![计算机中的光学三原色](./images/033.png "计算机中的光学三原色")

彩色图片信息存储，就是在单位像素点上的红、绿、蓝三色的颜色占比来表示及存储的，一般用 RGB 来表示，数值区间为 0~255。

![不同数值的三原色构成不同的颜色](./images/034.png "不同数值的三原色构成不同的颜色")

![三原色小结](./images/035.png "三原色小结")

##### 声音的存储

声音的存储形式采用的是波形信息存储，如下图的波形图中，每一个点位上都有一个相应的数值，用来存储不同的声音信息，如果峰点的 111 、谷点的 000 .

![声音的存储-波形图](./images/036.png "声音的存储-波形图")

不同音质的产生原理

![不同音质的产生原理](./images/037.png "不同音质的产生原理")

视频数据与声音类似，视频采用流数据，单位时间内帧率与分辨率 决定了视频数据的大小与质量。

![计算机的存储规则小结](./images/038.png "j计算机的存储规则小结")

### 数据类型

基本数据类型

![基本数据类型](./images/039.png "基本数据类型")

> **注一**：如果要定义 long 类型的变量，在数据值的后面需要加一个 **L** 作为后缀，L 可以是大写的，也可以是小写的。**不建议使用小写 l，容易与数字 1 混淆。**
>
> **注二**：如果要定义 float 类型的变量，在数据值的后面需要加一个 **F** 作为后缀，F 可以是大写的，也可以是小写的。**为了书写和记忆的一致性，建议 F 大写。**

![基本数据类型小结](./images/040.png "基本数据类型小结")

### 标识符

标识符，即给类、方法、变量等起的名字。

[阿里巴巴Java开发手册(黄山版).pdf](./files/Java开发手册(黄山版).pdf)

**标识符命名规则 ---硬性要求**

- 由数字、字母、下划线()和美元符($)组成
- 不能以数字开头
- 不能是关键字
- 区分大小写

小驼峰与大驼峰

![小驼峰与大驼峰](./images/041.png "小驼峰与大驼峰")

### 键盘录入(简介)

java 帮我们写好了很多的类，这些类需要我们在日常工作中去学习和使用，举例来说：其中 Scanner 这个类就可以用来接收键盘输入的**数字**（仅用于接收数字，输入字母或汉字会报错） 

![Scanner 类的引用示例](./images/042.png "Scanner 类的引用示例")

### IDEA

- IntelliJ IDEA
  IDEA 全称 Intellij IDEA，是用于 ]ava 语言开发的集成环境，它是业界公认的目前用于 Java 程序开发最好的工具。

- 集成环境

  把代码编写、编译、执行、调试等多种功能综合到一起的开发工具。

[idea 下载](https://www.jetbrains.com/idea/)

IDEA 项目结构介绍

**project（项目）          module（模块）          package（包）          class（类）**

![IDEA 项目结构介绍](./images/043.png "IDEA 项目结构介绍")

- 一个 **project（项目）**可以包含多个 **module（模块）**
- 一个 **module（模块）**可以有多个 **package（包）**
- 我们可以把 **package（包）**理解为文件夹，可以把相同类型的 “文件“ 放在同一个 **package（包）**里，如文字、图片、动画...
- 每个 **class（类）** 文件才是我们真正写代码的地方。

> 快捷键-代码补全（举例）
>
> - 输入 psvm 创建一个主 入口 main 方法
> - 输入 soup 创建一个打印语句

## 运算符

### 运算符与表达式

运算符，即对字面量或者变量进行操作的符号。

表达式，用运算符把字面量或者变量连接起来，使它符合 java 语法的式子就叫做表达式。

![运算符与表达式](./images/044.png "运算符与表达式")

### 算术运算符

| 符号 |    作用    |
| :--: | :--------: |
| `+`  |     加     |
| `-`  |     减     |
| `*`  |     乘     |
| `/`  |     除     |
| `%`  | 取余、取模 |

#### 类型转换

数字进行运算时，数据类型不一致不能运算，需要转成一样的，才能运算。

![数字相加](./images/045.png "数字相加")

类型转换的类型：

- 隐式类型转换（自动类型提示）

- 强制类型转换

![类型转换](./images/046.png "类型转换")

#### 隐式类型转换

把一个取值范围小的数值，转成取值范围大的数值

>--------------------------------------取值范围从小变大----------------------------------------->
>
>`byte  -->  short  -->  int  -->  long  -->  float  -->  double`

```java

// 举例：系统将隐式的（自动的）将整数 10 转换成 10.0 
int a = 10;
double b = a;
System.out.println(b); // 10.0

```

隐式转换的两种提升规则

- 取值范围小的 和 取值范围大的 进行运算，小的会先提升为大的，再进行运算
- byte short char 这三种类型的数据，在运算的时候，都会直接先提升为 int 类型，然后再参与计算

#### 强制类型转换

如果把一个 取值范围大 的数值，赋值给 取值范围小 的变量，**是不允许直接赋值的。**必须先进行**强制类型转换**。

>**格式** ：目标数据类型 变量名 = (目标数据类型)被强制转换的数据

```java

// 范例：
double a = 12.3;
int b = (int)a;
System.out.println(b); // 12

```

**注意**，强制转换时，如果超出强转类型的取值范围，会出现错误！！！

```java

// 下面的强转会报错
int a = 300;
byte b = byte(a); // 会出现错误！！！

```

#### 字符串的拼接

在一个`+`运算符的表达式中，存在字符串时，`+`就不再作为算术的“加”法，而是作为字符串的连接符使用。

```java
"123" + 123 = 123123 // 字符串拼接
```

如果存在连续的 `+` 操作时，执行规则为**从左到右逐个执行**。

``` java

1 + 99 + "岁" // "100岁" 而不是 "199岁"

"我" + 1 + 8 + "岁" // "我18岁" 而不是 "我9岁"
    
```

#### 字符的`+`操作

>当 **字符** `+` **字符** 或者 **字符**  `+` **数字** 时，会把字符通过 ASCII 码表**查询到对应的数字**再进行计算。

```java

char a = 'a';
char b = 'b';
System.out.println(a + 10); // 97 + 10 = 108
System.out.println(a + b); // 97 + 98 = 195

// 当存在字符串时，+ 仅用于拼接字符串
System.out.println('a' + "abc"); // "aabc"

```

### 自增自减运算符

| 运算符 | 符号 | 作用 |     说明     |
| :----: | :--: | :--: | :----------: |
|  自增  | `++` |  加  | 变量的值加 1 |
|  自减  | `--` |  减  | 变量的值减 1 |

自增/自减运算符，即可以放在变量的前面，也可以放在变量的后面

>当单独使用时，自增/自减运算符无论是放在变量前还是变量后，其结果都是一样的。

```java

int a = 10;
int b = 10;
a++;
++b;
System.out.println(a); // 11
System.out.println(b); // 11
System.out.println(a == b); // true

int c = 10;
c++;
System.out.println(c); // 11
++c;
System.out.println(c); // 12
c--;
System.out.println(c); // 11
--c;
System.out.println(c); // 10

```

>当参与计算时，自增/自减运算符放在变量前还是变量后，其结果遵循以下规律：
>
>- 放在变量**后**    ----    遵循 **先用后加** 的原则
>- 放在变量**前**    ----    遵循 **先加后用** 的原则

```java

// 放在变量后 --- 先用后加
int a = 10;
int b = a++;
System.out.println(b); // 10
System.out.println(a); // 11

// 放在变量前 --- 先加后用
int c = 10;
int d = ++c;
System.out.println(c); // 11
System.out.println(d); // 11
System.out.println(c == d); // true

// 综合练习
int x = 10;
int y = x++;
int z = ++x;
System.out.println(x); // 12
System.out.println(y); // 10
System.out.println(z); // 12

```

### 赋值运算符

| 符号 |    作用    |              说明              |
| :--: | :--------: | :----------------------------: |
| `=`  |    赋值    | int a = 10，将 10 赋值给变量 a |
| `+=` |  加后赋值  |   a += b，将 a + b 的值给 a    |
| `-=` |  减后赋值  |   a -= b，将 a - b 的值给 a    |
| `*=` |  乘后赋值  |   a *= b，将 a * b 的值给 a    |
| `/=` |  除后赋值  |  a /= b，将 a / b 的 商 给 a   |
| `%=` | 取余后赋值 |  a %= b，将 a / b的 余数 给 a  |

**注：**扩展的赋值运算符隐含了强制类型转换

### 关系运算符

关系运算符也叫做比较运算符

| 符号 |                             说明                             |
| :--: | :----------------------------------------------------------: |
| `==` | a == b，判断 a 和 b 的值是否相等，成立为 true，不成立为 false |
| `!=` | a != b，判断 a 和 b 的值是否不相等，成立为 true，不成立为 false |
| `>`  |    a  >b，判断 a 是否大于 b ，成立为 true，不成立为 false    |
| `>=` | a >= b，判断 a 是否大于等于 b ，成立为 true，不成立为 false  |
| `<`  |    a < b，判断 a 是否小于 b ，成立为 true，不成立为 false    |
| `<=` | a <= b，判断 a 是否小于等于 b ，成立为 true，不成立为 false  |

**注一：** 关系运算符的结果都是 boolean 类型，要么是 true，要么是 false。

**注二：** 运算符 `=` 与 `==` 不相同，前者是赋值而后者是比较关系。

### 逻辑运算符

| 符号 |   作用   |            说明            |
| :--: | :------: | :------------------------: |
| `|`  |    或    |  或者，有真则真，全假则假  |
| `&`  | 与（且） |  并且，全真则真，有假则假  |
| `!`  |    非    |            取反            |
| `^`  |   异或   | 相同为 false， 不同为 true |

#### 短路逻辑运算符

| 符号 |  作用  |                           说明                            |
| :--: | :----: | :-------------------------------------------------------: |
| `||` | 短路或 | 结果和`|`相同，但因逻辑短路会减少逻辑判断从而减少性能损耗 |
| `&&` | 短路与 | 结果和`&`相同，但因逻辑短路会减少逻辑判断从而减少性能损耗 |

```java

// 短路逻辑运算符具有短路效果
// 即，当左边的表达式能够确定最终的结果时，右边的就不再参与运行了
int a = 10;
int b = 10;
boolean result = ++a < 5 && ++b < 5;

System.out.println(result); // false
System.out.println(a); // 11
// 因为 a < 5 为 假， 已经足够确定 result 的结果，因此 && 右侧的 ++b 就不再运行了
System.out.println(b); // 10

```

![短路逻辑运算注意事项](./images/047.png "短路逻辑运算注意事项")

### 三元运算符

三元运算符又叫做三元表达式

>**格式：** 关系表达式 ? 表达式1 : 表达式2;

三元运算符的计算规则：

- 首先计算关系表达式的值（判断出该关系表达式是否成立： true or false）
- 如果值为 true，则执行 表达式1
- 如果值为 false，则执行 表达式2

### 运算符优先级

![运算符的优先级](./images/048.png "运算符的优先级")

简单记忆：小括号`()`优先于所有

## 判断和循环

### 流程控制语句

#### 顺序结构

java 代码**从上到下，从左到右**按顺序编译运行。

#### 分支结构

##### if 语句

if 在程序当中，就是用来进行判断的。

```java

// 格式一 ： 单条件判断
if (关系表达式) {
	语句体；
} 

// 格式二 ：双条件判断
if (关系表达式) {
	语句体1；
} else {
	语句体2；
} 

// 格式三 ：多条件判断
if (关系表达式1) {
	语句体1；
} else if (关系表达式2) {
	语句体2；
} else if (关系表达式3) {
	语句体3；
} 
 ...
else {
    
}
```

##### switch 语句

```java
switch(表达式) {
    case 值1:
        语句体1;
        break;
    case 值2:
        语句体2;
        break;
    ...
    default:
        语句体n + 1;
        break;
}
```

switch 语句格式说明:

- 表达式（将要匹配的值）：取值为 byte、short、int、char。JDK5 以后可以是枚举，JDK7 以后可以是 String。
- case：后面跟的是要和表达式进行比较的值(被匹配的值)。
- break：表示中断，结束的意思，用来结束 switch 语句。
- default：表示所有情况都不匹配的时候，就执行该处的内容和 if 语句的 else 相似。
- case 后面的值只能是字面量，不能是变量。
- case 给出的值不允许重复

注意事项：

1. default 可以省略但不建议省略，default 语句可以写在任何位置，但一般建议是写在末尾。
2. case穿透：当 case 穿透开始后，后续的 case 语句就不会有匹配作用，内部的语句都会执行。直到看见 break或者 switch 语句执行完毕后才会结束。
3. JDK12的新特性：switch 语句的简写模式

```java

// JDK12的新特性: 箭头写法
int number = 1;
switch (number) {
    case 1 -> {
        System.out.println("一");
    }
    case 2 -> {
        System.out.println("二");
    }
    case 1 -> {
        System.out.println("三");
    }
    default -> {
        System.out.println("没有这个选项");
    }
}

// 如果语句体只有一句，`{}`大括号可以省略

int number1 = 2;
switch (number1) {
    case 1 -> System.out.println("一");
    case 2 -> System.out.println("二");
    case 1 -> System.out.println("三");
    default -> System.out.println("没有这个选项");
}

```

switch 语句 和 if 语句的第三种格式，其各自的使用场景：

- if 语句的第三种格式：**一般用于对范围的判断**
- switch 语句：**把有限个数据一一列举出来，让我们任选其一**

switch 语句 练习：

```java
import java.util.Scanner;

public class Test8 {
    public static void main(String[] args) {
        // 需求:键盘录入星期数输出工作日、休息日。
        // (1-5)工作日，(6-7)休息日。

        // 1.记录键盘录入
        Scanner sc = new Scanner(System.in);
        System.out.println("请输入星期数");
        int week = sc.nextInt();

        // 2. 判断
        /* // case 穿透
        switch (week) {
            case 1:
            case 2:
            case 3:
            case 4:
            case 5:
                System.out.println("今天是工作日");
                break;
            case 6:
            case 7:
                System.out.println("今天是休息日");
                break;
            default:
                System.out.println("输入的星期数有误");
                break;
        } */

        /* // 再次简化
        switch (week) {
            case 1, 2, 3, 4, 5:
                System.out.println("今天是工作日");
                break;
            case 6, 7:
                System.out.println("今天是休息日");
                break;
            default:
                System.out.println("输入的星期数有误");
                break;
        } */
        
        // JDK12新特性
        switch (week) {
            case 1, 2, 3, 4, 5 -> System.out.println("今天是工作日");
            case 6, 7 -> System.out.println("今天是休息日");
            default -> System.out.println("输入的星期数有误");
        }
    }
}
```

#### 循环结构

定义： 重复的做某件事情，并且具有明确的开始和停止标记。

分类：

- for 循环
- while 循环
- do ... while 循环

##### for 循环

>格式：
>
>for(初始化语句; 条件判断语句; 条件控制语句) {
>
>​	循环体语句；
>
>}